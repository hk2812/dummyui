spring:
  application:
    name: config-server-testfiles
  profiles:
    active: development  
  cloud:
    config:
      uri: http://localhost:8888/admin
management:
  security:
    enabled: false       